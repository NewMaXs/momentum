import { Text } from "react-native-paper";
import { SafeAreaView } from "react-native-safe-area-context";

export default function FocusScreen() {
  return (
    <SafeAreaView>
      <Text>this is Focus page</Text>
    </SafeAreaView>
  );
}
